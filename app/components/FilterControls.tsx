import React from 'react';

const FilterControls: React.FC = () => {
  return (
    <div style={{ padding: '16px', marginBottom: '16px', border: '1px dashed #ccc', borderRadius: '8px' }}>
      <p>Filter controls will go here (e.g., by agency, deadline, eligibility).</p>
    </div>
  );
};

export default FilterControls;
